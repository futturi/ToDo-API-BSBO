# ✅ ToDo API

Это ToDo API — веб-приложение для управления задачами, использующее матрицу Эйзенхауэра для категоризации задач. 
Проект построен на современном фреймворке FastAPI и предоставляет RESTful API для операций CRUD (создание, чтение, обновление, удаление) над задачами.

🎯 Матрица Эйзенхауэра
Матрица делит задачи на 4 категории:
1. Срочно и Важно (Делать немедленно)
2. Не срочно, но Важно (Запланировать)
3. Срочно, но Не важно (Делегировать)
4. Не срочно и Не важно (Удалить)



## 🔧 Запуск

```bash
pip install fastapi uvicorn
uvicorn main:app --reload
```
Эндпоинты
```bash
GET    /                    # Приветствие и информация об API
GET    /health              # Проверка здоровья приложения
POST   /tasks              # Создание задачи (устаревший метод)

GET    /api/v1/tasks       # Получение всех задач
POST   /api/v1/tasks       # Создание новой задачи
GET    /api/v1/tasks/{id}  # Получение задачи по ID
PUT    /api/v1/tasks/{id}  # Обновление задачи
DELETE /api/v1/tasks/{id}  # Удаление задачи
PATCH  /api/v1/tasks/{id}/complete # Отметка как выполненной

GET    /api/v1/matrix-categories # Категории матрицы
```

Данное API предоставляет полный набор инструментов для управления задачами с использованием методологии Эйзенхауэра. Архитектура спроектирована для легкого расширения, код написан с соблюдением best practices для FastAPI.

Ключевые преимущества:

✅ Современный фреймворк FastAPI

✅ Автоматическая документация OpenAPI

✅ Валидация данных через Pydantic

✅ Чистая архитектура с разделением на роутеры

✅ Готовность к масштабированию


